<div class="btn-group pull-right">
  {{#link-to "users.new" class="btn btn-xs btn-default"}}<span class="glyphicon glyphicon-plus"></span>{{/link-to}}
</div>
<div class="clearfix"></div>

{{#each user in controller}}
  {{view Bandango.UserOnListView model=user}}
{{/each}}

<div class="align_center has_margin">
  <div class="small">{{length}} usuarios</div>
  {{#if hasMore}}
    <a href="#" class="btn btn-link full_width" {{action showMore}}>Más</a>
  {{/if}}
</div>
